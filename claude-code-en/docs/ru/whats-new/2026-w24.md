> ## Documentation Index
> Fetch the complete documentation index at: https://code.claude.com/docs/llms.txt
> Use this file to discover all available pages before exploring further.

# Неделя 24 · 8–12 июня 2026

> Переместите сеанс в новый каталог с помощью /cd, позвольте подагентам создавать собственных подагентов и устраняйте неисправности в конфигурации с помощью безопасного режима.

<div className="digest-meta">
  <span>Выпуски <a href="/docs/docs/en/changelog#2-1-166">v2.1.166 → v2.1.176</a></span>
  <span>3 функции · 8–12 июня</span>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">Переместите сеанс с помощью /cd</span>
    <span className="digest-feature-pill">v2.1.169</span>
  </div>

  <p className="digest-feature-lede">Новая команда <code>/cd</code> перемещает текущий сеанс в другой рабочий каталог без перестроения кэша подсказок: файл <code>CLAUDE.md</code> нового каталога добавляется в виде сообщения вместо замены системной подсказки. Сеанс переходит в хранилище проекта нового каталога, поэтому `--resume` и `--continue` находят его там. Claude предлагает вам доверять каталогу, если вы в нём раньше не работали.</p>

  <p className="digest-feature-try">Переместите сеанс в другой проект без перезагрузки:</p>

  ```text Claude Code theme={null}
  > /cd ../other-project
  ```

  <a className="digest-feature-link" href="/docs/ru/docs/commands#all-commands">Справочник команд</a>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">Подагенты могут создавать подагентов</span>
    <span className="digest-feature-pill">v2.1.172</span>
  </div>

  <p className="digest-feature-lede">Подагенты теперь могут создавать собственных подагентов. Панель подагентов под подсказкой показывает полное дерево: каждая строка содержит количество её потомков и путь обратно к <code>main</code>. Цепочки подагентов ограничены пятью уровнями в глубину, чтобы предотвратить неконтролируемые одновременные деревья.</p>

  <p className="digest-feature-try">Откройте представление агентов, чтобы наблюдать вложенное дерево по мере развёртывания работы:</p>

  ```text Claude Code theme={null}
  > /agents
  ```

  <a className="digest-feature-link" href="/docs/ru/docs/sub-agents#spawn-nested-subagents">Создание вложенных подагентов</a>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">Устранение неисправностей с помощью безопасного режима</span>
    <span className="digest-feature-pill">v2.1.169</span>
  </div>

  <p className="digest-feature-lede">Запустите Claude Code с `--safe-mode` или установите <code>CLAUDE\_CODE\_SAFE\_MODE</code>, чтобы запустить со всеми отключёнными настройками: <code>CLAUDE.md</code>, skills, plugins, hooks, MCP серверы и пользовательские команды и агенты не загружаются. Аутентификация, выбор модели, встроенные инструменты и разрешения по-прежнему работают. Если проблема исчезает в безопасном режиме, одна из этих поверхностей является причиной.</p>

  <p className="digest-feature-try">Запустите чистый сеанс, чтобы изолировать неисправную конфигурацию:</p>

  ```bash terminal theme={null}
  claude --safe-mode
  ```

  <a className="digest-feature-link" href="/docs/ru/docs/debug-your-config#test-against-a-clean-configuration">Тестирование с чистой конфигурацией</a>
</div>

<div className="digest-wins">
  <p className="digest-wins-title">Другие улучшения</p>

  <div className="digest-wins-grid">
    <div><a href="/docs/ru/docs/model-config#fallback-model-chains"><code>fallbackModel</code></a> настраивает до трёх резервных моделей, которые пробуются по порядку, когда основная перегружена или недоступна, и `--fallback-model` теперь применяется также к интерактивным сеансам</div>
    <div>Названия сеансов теперь генерируются на языке вашего разговора; закрепите конкретное с помощью параметра <code>language</code></div>
    <div>`claude agents --json` добавляет `--all` для включения завершённых сеансов плюс новые поля <code>id</code> и <code>state</code>, и больше не пропускает заблокированные или вновь отправленные сеансы</div>
    <div>Просмотр плагинов маркетплейса в <code>/plugin</code> теперь имеет строку поиска</div>
    <div>Новый параметр <code>disableBundledSkills</code> и <code>CLAUDE\_CODE\_DISABLE\_BUNDLED\_SKILLS</code> скрывают встроенные skills, workflows и встроенные команды от модели</div>
    <div>Правила отказа принимают glob в позиции имени инструмента, поэтому <code>"\*"</code> отказывает всем инструментам, и неизвестные имена инструментов в правилах отказа теперь предупреждают при запуске</div>
    <div>Обмен сообщениями между сеансами усилен: сообщения, переданные через <code>SendMessage</code> из других сеансов, больше не несут полномочия пользователя, и автоматический режим их блокирует</div>
    <div>Amazon Bedrock читает регион AWS из файлов конфигурации `~/.aws`, когда <code>AWS\_REGION</code> не установлен, и `/status` показывает, откуда взялся регион</div>
    <div>Новый управляемый параметр <code>enforceAvailableModels</code> делает список разрешений <code>availableModels</code> также ограничивающим модель по умолчанию</div>
    <div>Claude в инструментах браузера Chrome теперь загружаются в одном пакетном вызове вместо одного на инструмент</div>
    <div><code>claude update</code> объявляет целевую версию перед загрузкой вместо молчания</div>
    <div>Новый параметр <code>footerLinksRegexes</code> добавляет значки ссылок, соответствующие регулярному выражению, в строку нижнего колонтитула</div>
  </div>
</div>

[Полный журнал изменений для v2.1.166–v2.1.176 →](/docs/en/changelog#2-1-166)
