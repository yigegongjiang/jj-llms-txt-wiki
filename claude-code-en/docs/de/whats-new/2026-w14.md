> ## Documentation Index
> Fetch the complete documentation index at: https://code.claude.com/docs/llms.txt
> Use this file to discover all available pages before exploring further.

# Woche 14 · 30. März – 3. April 2026

> Computernutzung in der CLI, interaktive In-Product-Lektionen, flimmerfreies Rendering, MCP-Ergebnisgröße-Overrides pro Tool und Plugin-Ausführbare auf PATH.

<div className="digest-meta">
  <span>Releases <a href="/docs/docs/en/changelog#2-1-86">v2.1.86 → v2.1.91</a></span>
  <span>5 Features · 30. März – 3. April</span>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">Computernutzung in der CLI</span>
    <span className="digest-feature-pill">research preview</span>
  </div>

  <p className="digest-feature-lede">Letzte Woche landete die Computernutzung in der Desktop-App. Diese Woche ist sie in der CLI: Claude kann native Apps öffnen, durch die Benutzeroberfläche klicken, seine eigenen Änderungen testen und reparieren, was kaputt geht – alles von Ihrem Terminal aus. Web-Apps hatten bereits Verifikationsschleifen; native iOS-, macOS- und andere nur GUI-Apps nicht. Jetzt haben sie es. Am besten zum Schließen der Schleife bei Apps und Tools, bei denen es keine API zum Aufrufen gibt. Noch früh; erwarten Sie raue Kanten.</p>

  <Frame>
    <video autoPlay muted loop playsInline className="w-full" src="https://mintcdn.com/claude-code/CfffsX01JHFnIKvD/images/whats-new/cli-computer-use.mp4?fit=max&auto=format&n=CfffsX01JHFnIKvD&q=85&s=c17a337902308d7c9121013ded0494db" data-path="images/whats-new/cli-computer-use.mp4" />
  </Frame>

  <p className="digest-feature-try">Erfordert macOS und einen Pro- oder Max-Plan; andernfalls wird <code>computer-use</code> nicht in <code>/mcp</code> angezeigt. Führen Sie <code>/mcp</code> aus, finden Sie <code>computer-use</code> und schalten Sie es ein. Dann bitten Sie Claude, eine Änderung von Ende zu Ende zu überprüfen:</p>

  ```text Claude Code theme={null}
  > Open the iOS simulator, tap through onboarding, and screenshot each step
  ```

  <a className="digest-feature-link" href="/docs/de/docs/computer-use">Computernutzungs-Anleitung</a>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">/powerup</span>
    <span className="digest-feature-pill">v2.1.90</span>
  </div>

  <p className="digest-feature-lede">Interaktive Lektionen, die Claude Code-Funktionen durch animierte Demos direkt in Ihrem Terminal vermitteln. Claude Code wird häufig aktualisiert, und Funktionen, die letzte Woche Ihre Arbeitsweise geändert hätten, können übersehen werden. Führen Sie <code>/powerup</code> einmal aus und Sie wissen, was es gibt.</p>

  <Frame>
    <video autoPlay muted loop playsInline className="w-full" src="https://mintcdn.com/claude-code/CfffsX01JHFnIKvD/images/whats-new/powerup.mp4?fit=max&auto=format&n=CfffsX01JHFnIKvD&q=85&s=fb88beddc0ecc8029da5ab029e4b28f1" data-path="images/whats-new/powerup.mp4" />
  </Frame>

  <p className="digest-feature-try">Führen Sie es aus:</p>

  ```text Claude Code theme={null}
  > /powerup
  ```

  <a className="digest-feature-link" href="/docs/de/docs/commands">Befehls-Referenz</a>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">Flimmerfreies Rendering</span>
    <span className="digest-feature-pill">v2.1.89</span>
  </div>

  <p className="digest-feature-lede">Entscheiden Sie sich für einen neuen Alt-Screen-Renderer mit virtualisiertem Scrollback. Die Eingabeaufforderung bleibt am unteren Rand fixiert, die Mausauswahl funktioniert über lange Konversationen hinweg, und das Flimmern beim Neuzeichnen ist weg. Deaktivieren Sie <code>CLAUDE\_CODE\_NO\_FLICKER</code>, um zurückzurollen.</p>

  <Frame>
    <video autoPlay muted loop playsInline className="w-full" src="https://mintcdn.com/claude-code/CfffsX01JHFnIKvD/images/whats-new/flicker-free.mp4?fit=max&auto=format&n=CfffsX01JHFnIKvD&q=85&s=7719e35e52a3f9734b0cf69edac333ad" data-path="images/whats-new/flicker-free.mp4" />
  </Frame>

  <p className="digest-feature-try">Setzen Sie die Umgebungsvariable und starten Sie Claude Code neu:</p>

  ```bash theme={null}
  export CLAUDE_CODE_NO_FLICKER=1
  claude
  ```

  <a className="digest-feature-link" href="/docs/de/docs/fullscreen">Vollbildrendering</a>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">MCP-Ergebnisgröße-Override</span>
    <span className="digest-feature-pill">v2.1.91</span>
  </div>

  <p className="digest-feature-lede">MCP-Server-Autoren können jetzt die Kürzungsgrenze für ein bestimmtes Tool erhöhen, indem sie <code>anthropic/maxResultSizeChars</code> im <code>tools/list</code>-Eintrag des Tools setzen, bis zu einer harten Obergrenze von 500K Zeichen. Die Obergrenze war früher global, daher trafen Tools, die gelegentlich inhärent große Nutzlasten wie Datenbankschemas oder vollständige Dateibäume zurückgaben, auf die Standardgrenze und wurden auf der Festplatte mit einer Dateireferenz gespeichert. Pro-Tool-Overrides halten diese Ergebnisse inline, wenn das Tool sie wirklich braucht.</p>

  <p className="digest-feature-try">Kommentieren Sie das Tool in der <code>tools/list</code>-Antwort Ihres Servers:</p>

  ```json highlight={5} theme={null}
  {
    "name": "get_schema",
    "description": "Returns the full database schema",
    "_meta": {
      "anthropic/maxResultSizeChars": 500000
    }
  }
  ```

  <a className="digest-feature-link" href="/docs/de/docs/mcp#raise-the-limit-for-a-specific-tool">MCP-Referenz</a>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">Plugin-Ausführbare auf PATH</span>
    <span className="digest-feature-pill">v2.1.91</span>
  </div>

  <p className="digest-feature-lede">Platzieren Sie eine ausführbare Datei in einem <code>bin/</code>-Verzeichnis in Ihrem Plugin-Root und Claude Code fügt dieses Verzeichnis zum <code>PATH</code> des Bash-Tools hinzu, während das Plugin aktiviert ist. Claude kann dann die Binärdatei als bloßen Befehl aus jedem Bash-Tool-Aufruf aufrufen, ohne dass ein absoluter Pfad oder ein Wrapper-Skript erforderlich ist. Praktisch zum Verpacken von CLI-Helfern neben den Befehlen, Agenten und Hooks, die sie aufrufen.</p>

  <p className="digest-feature-try">Fügen Sie ein <code>bin/</code>-Verzeichnis im Plugin-Root hinzu:</p>

  ```text highlight={4, 5} theme={null}
  my-plugin/
  ├── .claude-plugin/
  │   └── plugin.json
  └── bin/
      └── my-tool
  ```

  <a className="digest-feature-link" href="/docs/de/docs/plugins-reference#file-locations-reference">Plugins-Referenz</a>
</div>

<div className="digest-wins">
  <p className="digest-wins-title">Weitere Erfolge</p>

  <div className="digest-wins-grid">
    <div>Auto-Modus-Folgefragen: neuer <code>PermissionDenied</code>-Hook wird bei Klassifizierer-Ablehnungen ausgelöst (geben Sie <code>retry: true</code> zurück, um Claude einen anderen Ansatz versuchen zu lassen), und <code>/permissions</code> → Kürzlich abgelehnt ermöglicht es Ihnen, manuell mit <code>r</code> erneut zu versuchen</div>
    <div>Neuer <code>defer</code>-Wert für <code>permissionDecision</code> in <code>PreToolUse</code>-Hooks: <code>-p</code>-Sitzungen pausieren bei einem Tool-Aufruf und beenden sich mit einer <code>deferred\_tool\_use</code>-Nutzlast, damit eine SDK-App oder benutzerdefinierte Benutzeroberfläche sie anzeigen kann, dann mit <code>--resume</code> fortgesetzt werden</div>
    <div><code>/buddy</code>: schlüpfen Sie ein kleines Wesen aus, das Sie beim Codieren beobachtet. Ein April-Fools'-Scherz, nicht mehr verfügbar</div>
    <div><code>disableSkillShellExecution</code>-Einstellung blockiert Inline-Shell von Skills, Slash-Befehlen und Plugin-Befehlen</div>
    <div>Edit-Tool funktioniert jetzt auf Dateien, die über <code>cat</code> oder <code>sed -n</code> angezeigt werden, ohne einen separaten Read</div>
    <div>Hook-Ausgabe über 50K wird auf der Festplatte mit einem Pfad + Vorschau gespeichert, anstatt in den Kontext eingefügt zu werden</div>
    <div>Thinking-Zusammenfassungen sind standardmäßig in interaktiven Sitzungen deaktiviert (<code>showThinkingSummaries: true</code> zum Wiederherstellen)</div>
    <div>Voice-Modus: Push-to-Talk-Modifizierer-Kombinationen, Windows WebSocket, macOS Apple Silicon-Mikrofon-Berechtigung</div>
    <div><code>claude-cli://</code> Deep Links akzeptieren mehrzeilige Eingabeaufforderungen (kodiert <code>%0A</code>)</div>
  </div>
</div>

[Vollständiges Changelog für v2.1.86–v2.1.91 →](/docs/en/changelog#2-1-86)
