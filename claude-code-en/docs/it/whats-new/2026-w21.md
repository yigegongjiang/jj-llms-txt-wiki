> ## Documentation Index
> Fetch the complete documentation index at: https://code.claude.com/docs/llms.txt
> Use this file to discover all available pages before exploring further.

# Settimana 21 · 18–22 maggio 2026

> Utilizza la modalità auto nel piano Pro e con Sonnet 4.6, visualizza quali skills, subagents e server MCP determinano i limiti del vostro piano in /usage, e rivedi i diff con il nuovo comando /code-review.

<div className="digest-meta">
  <span>Releases <a href="/docs/docs/en/changelog#2-1-143">v2.1.143 → v2.1.149</a></span>
  <span>1 feature · 18–22 maggio</span>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">Modalità auto nel piano Pro</span>
    <span className="digest-feature-pill">CLI</span>
  </div>

  <p className="digest-feature-lede">La modalità auto è ora disponibile nel piano Pro e supporta Sonnet 4.6 insieme a Opus. Sostituisce i prompt di autorizzazione con controlli di sicurezza in background: le azioni di routine vengono eseguite senza interrompervi, e quelle distruttive o sospette vengono bloccate e segnalate.</p>

  <p className="digest-feature-try">Aggiornate Claude Code, quindi alternate le modalità con Shift+Tab; la modalità auto appare una volta che il vostro account soddisfa i requisiti:</p>

  ```bash terminal theme={null}
  claude update
  ```

  <a className="digest-feature-link" href="/docs/it/permission-modes#eliminate-prompts-with-auto-mode">Modalità auto</a>
</div>

<div className="digest-wins">
  <p className="digest-wins-title">Altri vantaggi</p>

  <div className="digest-wins-grid">
    <div><a href="/docs/it/costs#track-your-costs"><code>/usage</code></a> ora mostra una suddivisione per categoria di ciò che determina i limiti del vostro piano, attribuendo l'utilizzo recente a skills, subagents, plugins e singoli server MCP</div>
    <div>"Extra usage" è rinominato in "usage credits" in tutta la CLI, e <code>/extra-usage</code> è ora <code>/usage-credits</code>. Il nome precedente continua a funzionare.</div>
    <div>Nuovo comando <a href="/docs/it/code-review"><code>/code-review</code></a> che segnala bug di correttezza a un livello di sforzo scelto come <code>/code-review high</code>, e <code>--comment</code> pubblica i risultati come commenti inline su GitHub PR. <code>/simplify</code> rimane come revisione separata solo per la pulizia.</div>
    <div>Le sessioni in background ora appaiono in <code>/resume</code> insieme a quelle interattive, contrassegnate con <code>bg</code>, e le sessioni fissate con <code>Ctrl+T</code> in <code>claude agents</code> rimangono attive quando inattive</div>
    <div><code>claude agents --json</code> elenca le sessioni attive come JSON per lo scripting, come barre di stato e selettori di sessione</div>
    <div>Lo strumento PowerShell è ora abilitato per impostazione predefinita su Windows per gli utenti di Amazon Bedrock, Google Cloud's Agent Platform e Microsoft Foundry; disabilitate con <code>CLAUDE\_CODE\_USE\_POWERSHELL\_TOOL=0</code></div>
    <div><code>claude plugin disable</code> ora rifiuta quando un altro plugin abilitato dipende dal target, e <code>claude plugin enable</code> abilita forzatamente le dipendenze transitive</div>
    <div>Il riquadro di esplorazione del marketplace <code>/plugin</code> mostra il costo del contesto previsto, e le schermate Discover e Browse elencano i comandi, gli agenti, le skills, gli hooks e i server MCP/LSP di un plugin prima dell'installazione</div>
    <div>La nuova impostazione <code>worktree.bgIsolation: "none"</code> consente alle sessioni in background di modificare direttamente la copia di lavoro senza <code>EnterWorktree</code>, per i repository in cui i worktrees non sono pratici</div>
    <div>L'output Markdown renderizza le caselle di controllo dell'elenco attività GFM, e la vista dettagli <code>/diff</code> scorre con la tastiera</div>
    <div>L'input JSON della riga di stato ora include informazioni sul repository GitHub e PR quando rilevate</div>
    <div>Enterprise: l'impostazione gestita <code>allowAllClaudeAiMcps</code> carica i connettori cloud MCP di claude.ai insieme a <code>managed-mcp.json</code></div>
  </div>
</div>

[Changelog completo per v2.1.143–v2.1.149 →](/docs/en/changelog#2-1-143)
