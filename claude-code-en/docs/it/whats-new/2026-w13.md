> ## Documentation Index
> Fetch the complete documentation index at: https://code.claude.com/docs/llms.txt
> Use this file to discover all available pages before exploring further.

# Settimana 13 · 23–27 marzo 2026

> Modalità auto per permessi senza intervento, computer use integrato, auto-fix PR nel cloud, ricerca trascrizioni e uno strumento PowerShell per Windows.

<div className="digest-meta">
  <span>Rilasci <a href="/docs/docs/en/changelog#2-1-83">v2.1.83 → v2.1.85</a></span>
  <span>6 funzionalità · 23–27 marzo</span>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">Auto mode</span>
    <span className="digest-feature-pill">research preview</span>
  </div>

  <p className="digest-feature-lede">Auto mode affida i vostri prompt di permesso a un classificatore. Le modifiche e i comandi sicuri vengono eseguiti senza interrompervi; qualsiasi cosa distruttiva o sospetta viene bloccata e segnalata. È il compromesso tra approvare ogni scrittura di file e eseguire con <code>--dangerously-skip-permissions</code>.</p>

  <Frame>
    <img src="https://mintcdn.com/claude-code/CfffsX01JHFnIKvD/images/whats-new/auto-mode.png?fit=max&auto=format&n=CfffsX01JHFnIKvD&q=85&s=367c9e9d4ba5bc57ec4b935154bf1fbb" alt="Claude Code prompt footer showing 'auto mode on (shift+tab to cycle)' indicator in yellow" width="2400" height="691" data-path="images/whats-new/auto-mode.png" />
  </Frame>

  <p className="digest-feature-try">Passate a auto con Shift+Tab, oppure impostatelo come predefinito:</p>

  ```json ~/.claude/settings.json {3} theme={null}
  {
    "permissions": {
      "defaultMode": "auto"
    }
  }
  ```

  <a className="digest-feature-link" href="/docs/it/docs/permission-modes">Guida alle modalità di permesso</a>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">Computer use</span>
    <span className="digest-feature-pill">Desktop</span>
  </div>

  <p className="digest-feature-lede">Claude può ora controllare il vostro desktop effettivo dall'app Claude Code Desktop: aprire app native, fare clic attraverso il simulatore iOS, controllare i pannelli di controllo hardware e verificare le modifiche sullo schermo. È disattivato per impostazione predefinita e chiede prima di ogni azione. Ideale per le cose che nient'altro può raggiungere: app senza API, strumenti proprietari, qualsiasi cosa che esista solo come GUI.</p>

  <Frame>
    <img src="https://mintcdn.com/claude-code/CfffsX01JHFnIKvD/images/whats-new/computer-use.png?fit=max&auto=format&n=CfffsX01JHFnIKvD&q=85&s=d631de2017edafff463505f8ddbc0f51" alt="Claude Desktop settings with the Computer use toggle enabled, showing the option to let Claude take screenshots and control your keyboard and mouse in apps you allow" width="2376" height="1210" data-path="images/whats-new/computer-use.png" />
  </Frame>

  <p className="digest-feature-try">Abilitatelo in Impostazioni, concedete i permessi del sistema operativo, quindi chiedete a Claude di verificare una modifica da capo a fondo:</p>

  ```text Claude Code theme={null}
  > Open the iOS simulator, tap through the onboarding flow, and screenshot each step
  ```

  <a className="digest-feature-link" href="/docs/it/docs/desktop#let-claude-use-your-computer">Guida al computer use</a>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">PR auto-fix</span>
    <span className="digest-feature-pill">Web</span>
  </div>

  <p className="digest-feature-lede">Attivate un interruttore quando aprite una PR e allontanatevi. Claude monitora CI, corregge i guasti, gestisce i dettagli e spinge fino a quando non diventa verde. Non più babysitting di una PR attraverso sei round di errori di lint.</p>

  <Frame>
    <img src="https://mintcdn.com/claude-code/CfffsX01JHFnIKvD/images/whats-new/auto-fix.png?fit=max&auto=format&n=CfffsX01JHFnIKvD&q=85&s=c62b181c6c5d96929f0b43525f9f3584" alt="Claude Code web CI panel showing the Auto fix toggle enabled, with description 'Proactively fix CI failures and review comments'" width="960" height="444" data-path="images/whats-new/auto-fix.png" />
  </Frame>

  <p className="digest-feature-try">Dopo aver creato una PR su Claude Code web, attivate Auto fix nel pannello CI.</p>

  <a className="digest-feature-link" href="/docs/it/docs/claude-code-on-the-web#auto-fix-pull-requests">Auto-fix pull requests</a>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">Transcript search</span>
    <span className="digest-feature-pill">v2.1.83</span>
  </div>

  <p className="digest-feature-lede">Premete <code>/</code> in modalità trascrizione per cercare la vostra conversazione. <code>n</code> e <code>N</code> scorrono i risultati. Finalmente un modo per trovare quel comando Bash che Claude ha eseguito 400 messaggi fa.</p>

  <p className="digest-feature-try">Aprite la modalità trascrizione e cercate:</p>

  ```text Claude Code theme={null}
  Ctrl+O    # open transcript
  /migrate  # search for "migrate"
  n         # next match
  N         # previous match
  ```

  <a className="digest-feature-link" href="/docs/it/docs/fullscreen#search-and-review-the-conversation">Guida fullscreen</a>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">PowerShell tool</span>
    <span className="digest-feature-pill">preview</span>
    <span className="digest-feature-pill">v2.1.84</span>
  </div>

  <p className="digest-feature-lede">Windows ottiene uno strumento PowerShell nativo insieme a Bash. Claude può eseguire cmdlet, eseguire pipe di oggetti e lavorare con percorsi nativi di Windows senza tradurre tutto attraverso Git Bash.</p>

  <p className="digest-feature-try">Accettate dalle impostazioni:</p>

  ```json .claude/settings.json {3} theme={null}
  {
    "env": {
      "CLAUDE_CODE_USE_POWERSHELL_TOOL": "1"
    }
  }
  ```

  <a className="digest-feature-link" href="/docs/it/docs/tools-reference#powershell-tool">Documentazione PowerShell tool</a>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">Conditional hooks</span>
    <span className="digest-feature-pill">v2.1.85</span>
  </div>

  <p className="digest-feature-lede">Gli hooks possono ora dichiarare un campo <code>if</code> utilizzando la sintassi delle regole di permesso. Il vostro controllo pre-commit viene generato solo per <code>Bash(git commit \*)</code> invece che per ogni chiamata bash, riducendo l'overhead del processo su sessioni occupate.</p>

  <p className="digest-feature-try">Limitate un hook ai soli commit git:</p>

  ```json .claude/settings.json {5} theme={null}
  {
    "hooks": {
      "PreToolUse": [{
        "hooks": [{
          "if": "Bash(git commit *)",
          "type": "command",
          "command": ".claude/hooks/lint-staged.sh"
        }]
      }]
    }
  }
  ```

  <a className="digest-feature-link" href="/docs/it/docs/hooks">Riferimento hooks</a>
</div>

<div className="digest-wins">
  <p className="digest-wins-title">Altri successi</p>

  <div className="digest-wins-grid">
    <div>Plugin <code>userConfig</code> ora pubblico: richiedere impostazioni al momento dell'abilitazione, segreti supportati da keychain</div>
    <div>Le immagini incollate inseriscono chip <code>\[Image #N]</code> che potete referenziare posizionalmente</div>
    <div>Directory drop-in <code>managed-settings.d/</code> per frammenti di policy stratificati</div>
    <div>Eventi hook <code>CwdChanged</code> e <code>FileChanged</code> per configurazioni di tipo direnv</div>
    <div>Gli agenti possono dichiarare <code>initialPrompt</code> nel frontmatter per inviare automaticamente un primo turno</div>
    <div><code>Ctrl+X Ctrl+E</code> apre il vostro editor esterno, corrispondendo a readline</div>
    <div>L'interruzione prima di qualsiasi risposta ripristina automaticamente il vostro input</div>
    <div><code>/status</code> ora funziona mentre Claude sta rispondendo</div>
    <div>I deep link si aprono nel vostro terminale preferito, non nel primo rilevato</div>
    <div>Nudge di ritorno inattivo a <code>/clear</code> dopo 75+ minuti di assenza</div>
    <div>VS Code: banner di limite di velocità, picker di rewind Esc-due volte</div>
  </div>
</div>

[Changelog completo per v2.1.83–v2.1.85 →](/docs/en/changelog#2-1-83)
