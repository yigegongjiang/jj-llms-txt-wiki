> ## Documentation Index
> Fetch the complete documentation index at: https://code.claude.com/docs/llms.txt
> Use this file to discover all available pages before exploring further.

# Semana 18 · 27 de abril – 1 de mayo de 2026

> Claude Code en Windows se ejecuta sin Git Bash, claude auth login acepta un código OAuth pegado cuando la devolución de llamada del navegador no puede alcanzar localhost, claude project purge limpia el estado local por proyecto, y pegar una URL de PR en /resume encuentra la sesión que la creó.

<div className="digest-meta">
  <span>Releases <a href="/docs/docs/en/changelog#2-1-120">v2.1.120 → v2.1.126</a></span>
  <span>4 características · 27 de abril – 1 de mayo</span>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">Iniciar sesión sin devolución de llamada del navegador</span>
    <span className="digest-feature-pill">v2.1.126</span>
  </div>

  <p className="digest-feature-lede"><code>claude auth login</code> ahora acepta el código OAuth pegado directamente en la terminal cuando la devolución de llamada del navegador no puede alcanzar localhost. Esto cubre WSL2, sesiones SSH y contenedores, donde la redirección a un puerto local no funciona. La misma versión también corrige los tiempos de espera de inicio de sesión en conexiones lentas o con proxy y en devcontainers solo IPv6.</p>

  <p className="digest-feature-try">Inicie sesión y luego pegue el código del navegador:</p>

  ```bash theme={null}
  claude auth login
  ```

  <a className="digest-feature-link" href="/docs/docs/es/cli-reference#cli-commands">Referencia de CLI</a>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">claude project purge</span>
    <span className="digest-feature-pill">v2.1.124</span>
  </div>

  <p className="digest-feature-lede">Elimine todo el estado de Claude Code para un proyecto: transcripciones, tareas, historial de archivos y la entrada de configuración del proyecto. Admite `--dry-run` para obtener una vista previa, `-y`/`--yes` para omitir la confirmación, `-i`/`--interactive` para elegir, y `--all` para limpiar todos los proyectos.</p>

  <p className="digest-feature-try">Obtenga una vista previa de lo que se eliminaría:</p>

  ```bash theme={null}
  claude project purge --dry-run
  ```

  <p className="digest-feature-try">Luego ejecútelo de verdad:</p>

  ```bash theme={null}
  claude project purge
  ```

  <a className="digest-feature-link" href="/docs/docs/es/cli-reference">Referencia de CLI</a>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">Reanudar por URL de PR</span>
    <span className="digest-feature-pill">v2.1.122</span>
  </div>

  <p className="digest-feature-lede">Cuando crea una solicitud de extracción con <code>gh pr create</code>, Claude Code la vincula a la sesión que la produjo. Ahora puede volver a esa sesión solo desde la URL de PR, sin necesidad de recordar su nombre.</p>

  <p className="digest-feature-try">Abra el selector de sesiones:</p>

  ```text Claude Code theme={null}
  > /resume
  ```

  <p className="digest-feature-try">Pegue la URL de PR en el selector. El primer carácter del pegado lo coloca en modo de búsqueda, y la lista se filtra a la sesión que creó ese PR. Presione Intro para reanudarlo. Las URL de solicitudes de extracción y fusión de GitHub, GitHub Enterprise, GitLab y Bitbucket funcionan todas.</p>

  ```text Claude Code theme={null}
  https://github.com/your-org/your-repo/pull/1234
  ```

  <p className="digest-feature-try">Para omitir el selector, pase el número de PR en la línea de comandos en su lugar:</p>

  ```bash theme={null}
  claude --from-pr 1234
  ```

  <a className="digest-feature-link" href="/docs/docs/es/sessions#use-the-session-picker">Sesiones: usar el selector de sesiones</a>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">Windows sin Git Bash</span>
    <span className="digest-feature-pill">Windows</span>
  </div>

  <p className="digest-feature-lede">Git para Windows ya no es obligatorio. Cuando Bash no está presente, Claude Code usa PowerShell como herramienta de shell, y cuando la herramienta PowerShell está habilitada se trata como el shell principal. PowerShell 7 instalado a través de Microsoft Store, MSI sin PATH, o una herramienta global de <code>.NET</code> ahora se detecta automáticamente.</p>

  <a className="digest-feature-link" href="/docs/docs/es/setup">Guía de configuración</a>
</div>

<div className="digest-wins">
  <p className="digest-wins-title">Otros logros</p>

  <div className="digest-wins-grid">
    <div>Los servidores MCP pueden optar por no participar en el aplazamiento de búsqueda de herramientas con <code>alwaysLoad: true</code> en su configuración para que todas las herramientas de ese servidor siempre estén disponibles</div>
    <div>Nuevo <code>claude plugin prune</code> elimina dependencias de complementos instalados automáticamente huérfanos, y <code>plugin uninstall --prune</code> se propaga</div>
    <div><code>/skills</code> ahora tiene un cuadro de búsqueda de escritura para filtrar para que pueda encontrar una habilidad en una lista larga sin desplazarse</div>
    <div>Los hooks <code>PostToolUse</code> pueden reemplazar la salida de herramientas para cualquier herramienta a través de <code>hookSpecificOutput.updatedToolOutput</code>, no solo herramientas MCP</div>
    <div>Nuevo subcomando <a href="/docs/docs/es/ultrareview"><code>claude ultrareview</code></a> ejecuta <code>/ultrareview</code> de forma no interactiva desde CI o scripts: imprime hallazgos en stdout (<code>--json</code> para salida sin procesar) y sale con 0 al completarse o 1 en caso de fallo</div>
    <div><code>--dangerously-skip-permissions</code> ahora omite las indicaciones para escrituras en <code>.claude/</code>, <code>.git/</code>, <code>.vscode/</code>, archivos de configuración de shell y otras rutas previamente protegidas, mientras que los comandos de eliminación catastrófica aún solicitan como red de seguridad</div>
    <div>El selector <code>/model</code> puede enumerar modelos desde el punto final <code>/v1/models</code> de su puerta de enlace cuando <code>ANTHROPIC\_BASE\_URL</code> apunta a una puerta de enlace compatible con Anthropic; opte por <code>CLAUDE\_CODE\_ENABLE\_GATEWAY\_MODEL\_DISCOVERY=1</code> desde v2.1.129</div>
    <div>Los servidores MCP que encuentran un error transitorio durante el inicio ahora se reintentan automáticamente hasta 3 veces en lugar de permanecer desconectados</div>
    <div><code>ANTHROPIC\_BEDROCK\_SERVICE\_TIER</code> selecciona un nivel de servicio de Amazon Bedrock: <code>default</code>, <code>flex</code>, o <code>priority</code></div>
    <div><code>/terminal-setup</code> habilita la configuración de acceso al portapapeles de iTerm2 para que <code>/copy</code> funcione, incluso desde tmux</div>
    <div>La Plataforma de Agentes de Google Cloud ahora admite Federación de Identidad de Carga de Trabajo basada en certificados X.509 (mTLS ADC)</div>
    <div>Correcciones significativas de fugas de memoria: sesiones con muchas imágenes, <code>/usage</code> en historiales de transcripción grandes, y herramientas de larga duración sin eventos de progreso</div>
  </div>
</div>

[Registro de cambios completo para v2.1.120–v2.1.126 →](/docs/en/changelog#2-1-120)
