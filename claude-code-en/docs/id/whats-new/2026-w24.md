> ## Documentation Index
> Fetch the complete documentation index at: https://code.claude.com/docs/llms.txt
> Use this file to discover all available pages before exploring further.

# Minggu 24 · 8–12 Juni 2026

> Pindahkan sesi ke direktori baru dengan /cd, biarkan sub-agen menjalankan sub-agen mereka sendiri, dan selesaikan konfigurasi yang rusak dengan mode aman.

<div className="digest-meta">
  <span>Rilis <a href="/docs/docs/en/changelog#2-1-166">v2.1.166 → v2.1.176</a></span>
  <span>3 fitur · 8–12 Juni</span>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">Pindahkan sesi dengan /cd</span>
    <span className="digest-feature-pill">v2.1.169</span>
  </div>

  <p className="digest-feature-lede">Perintah <code>/cd</code> baru memindahkan sesi saat ini ke direktori kerja yang berbeda tanpa membangun ulang cache prompt: <code>CLAUDE.md</code> direktori baru ditambahkan sebagai pesan alih-alih mengganti prompt sistem. Sesi berpindah ke penyimpanan proyek direktori baru, jadi `--resume` dan `--continue` menemukannya di sana. Claude meminta Anda untuk mempercayai direktori jika Anda belum pernah bekerja di dalamnya sebelumnya.</p>

  <p className="digest-feature-try">Pindahkan sesi ke proyek lain tanpa memulai ulang:</p>

  ```text Claude Code theme={null}
  > /cd ../other-project
  ```

  <a className="digest-feature-link" href="/docs/docs/id/commands#all-commands">Referensi perintah</a>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">Sub-agen dapat menjalankan sub-agen</span>
    <span className="digest-feature-pill">v2.1.172</span>
  </div>

  <p className="digest-feature-lede">Sub-agen sekarang dapat menjalankan sub-agen mereka sendiri. Panel subagen di bawah prompt menunjukkan pohon lengkap: setiap baris membawa hitungan keturunannya dan jalur kembali ke <code>main</code>. Rantai sub-agen dibatasi pada lima tingkat kedalaman untuk mencegah pohon bersamaan yang liar.</p>

  <p className="digest-feature-try">Buka tampilan agen untuk menonton pohon bersarang saat pekerjaan menyebar:</p>

  ```text Claude Code theme={null}
  > /agents
  ```

  <a className="digest-feature-link" href="/docs/docs/id/sub-agents#spawn-nested-subagents">Jalankan subagen bersarang</a>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">Selesaikan dengan mode aman</span>
    <span className="digest-feature-pill">v2.1.169</span>
  </div>

  <p className="digest-feature-lede">Mulai Claude Code dengan `--safe-mode`, atau atur <code>CLAUDE\_CODE\_SAFE\_MODE</code>, untuk meluncurkan dengan semua kustomisasi dinonaktifkan: <code>CLAUDE.md</code>, skills, plugins, hooks, server MCP, dan perintah dan agen khusus tidak dimuat. Autentikasi, pemilihan model, alat bawaan, dan izin masih berfungsi. Jika masalah hilang dalam mode aman, salah satu permukaan tersebut adalah penyebabnya.</p>

  <p className="digest-feature-try">Luncurkan sesi bersih untuk mengisolasi konfigurasi yang rusak:</p>

  ```bash terminal theme={null}
  claude --safe-mode
  ```

  <a className="digest-feature-link" href="/docs/docs/id/debug-your-config#test-against-a-clean-configuration">Uji terhadap konfigurasi bersih</a>
</div>

<div className="digest-wins">
  <p className="digest-wins-title">Kemenangan lainnya</p>

  <div className="digest-wins-grid">
    <div><a href="/docs/docs/id/model-config#fallback-model-chains"><code>fallbackModel</code></a> mengonfigurasi hingga tiga model fallback yang dicoba secara berurutan ketika model utama kelebihan beban atau tidak tersedia, dan `--fallback-model` sekarang juga berlaku untuk sesi interaktif</div>
    <div>Judul sesi sekarang dihasilkan dalam bahasa percakapan Anda; pin satu tertentu dengan pengaturan <code>language</code></div>
    <div>`claude agents --json` menambahkan `--all` untuk menyertakan sesi yang selesai ditambah bidang <code>id</code> dan <code>state</code> baru, dan tidak lagi menghilangkan sesi yang diblokir atau baru dikirim</div>
    <div>Menjelajahi plugin marketplace di <code>/plugin</code> sekarang memiliki bilah pencarian</div>
    <div>Pengaturan <code>disableBundledSkills</code> baru dan <code>CLAUDE\_CODE\_DISABLE\_BUNDLED\_SKILLS</code> menyembunyikan skills bundel, alur kerja, dan perintah bawaan dari model</div>
    <div>Aturan penolakan menerima glob dalam posisi nama alat, jadi <code>"\*"</code> menolak semua alat, dan nama alat yang tidak dikenal dalam aturan penolakan sekarang memperingatkan saat startup</div>
    <div>Pesan lintas sesi diperkuat: pesan yang diteruskan melalui <code>SendMessage</code> dari sesi lain tidak lagi membawa otoritas pengguna, dan mode otomatis memblokir mereka</div>
    <div>Amazon Bedrock membaca wilayah AWS dari file konfigurasi `~/.aws` ketika <code>AWS\_REGION</code> tidak diatur, dan `/status` menunjukkan dari mana wilayah berasal</div>
    <div>Pengaturan terkelola <code>enforceAvailableModels</code> baru membuat daftar allowlist <code>availableModels</code> juga membatasi model Default</div>
    <div>Claude dalam alat browser Chrome sekarang dimuat dalam satu panggilan batch tunggal alih-alih satu per alat</div>
    <div><code>claude update</code> mengumumkan versi target sebelum mengunduh alih-alih diam</div>
    <div>Pengaturan <code>footerLinksRegexes</code> baru menambahkan lencana tautan yang cocok regex ke baris footer</div>
  </div>
</div>

[Changelog lengkap untuk v2.1.166–v2.1.176 →](/docs/en/changelog#2-1-166)
